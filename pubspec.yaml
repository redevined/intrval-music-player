name: intrval_music_player
description: "intrval - a tempo-controlled practice-set music player for dancers."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 0.8.1+16

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^2.6.1

  # Local database (Songs / Playlists / Folders / Sets / Set Entries)
  drift: ^2.20.3
  sqlite3_flutter_libs: ^0.5.26
  path_provider: ^2.1.5
  path: ^1.9.0

  # File/folder access (Storage Access Framework for local device storage)
  saf: ^2.1.0
  permission_handler: ^11.3.1

  # Audio metadata extraction (title/artist/album/duration). Pure Dart - no
  # native Android/iOS module, unlike audiotags (see ADR 0008).
  audio_metadata_reader: ^1.7.1

  # Playback engine (libmpv + Rubber Band time-stretching) + its own
  # background/lock-screen media session - see ADR on the Sonic -> Rubber
  # Band tempo-engine migration.
  mpv_audio_kit: ^0.4.4

  # On-device BPM detection: extracts an amplitude envelope from the audio
  # file, which we then run a beat-periodicity (autocorrelation) analysis on.
  just_waveform: ^0.0.6

  # Utilities
  uuid: ^4.5.1
  collection: ^1.19.0
  shared_preferences: ^2.5.5
  package_info_plus: ^8.1.0
  url_launcher: ^6.3.1

  # System (Material You / Android 12+) seed color, for the app-icon
  # theme-cycling easter egg - see core/theme.dart. material_color_utilities
  # is a transitive dependency of dynamic_color already, but CorePalette
  # (its return type for DynamicColorPlugin.getCorePalette()) needs to be
  # importable directly.
  dynamic_color: ^1.8.1
  material_color_utilities: ^0.13.0

# jni 1.0.1 (a transitive dep of path_provider_android, pulled in regardless
# of the sqlite3 version) shipped a breaking Kotlin Gradle Plugin migration in
# its android/build.gradle that fails with "Could not find method kotlin()"
# on this project's Gradle/AGP/Kotlin versions. jni 1.0.2 reverts that
# migration - pin to it explicitly in case pub's resolver would otherwise
# pick 1.0.1 for some transitive path.
dependency_overrides:
  jni: 1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  drift_dev: ^2.20.3
  build_runner: ^2.4.13
  flutter_launcher_icons: ^0.14.4

# Launcher icons are generated by running:
#   dart run flutter_launcher_icons
#
# icon_crop.png is the full-bleed source artwork (no frame). icon_launcher.png
# is a plain 1024x1024 resize of it, used for generation.
#
# The adaptive background matches the artwork's paper tone, so the inset
# foreground blends into it instead of reading as a square-on-a-circle.
flutter_launcher_icons:
  image_path: "icon_launcher.png"
  android: true
  min_sdk_android: 23
  adaptive_icon_background: "#EAE8DB"
  adaptive_icon_foreground: "icon_launcher.png"
  ios: true
  remove_alpha_ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/app_icon_rounded.png
    - assets/sounds/beeps.mp3
    - assets/sounds/break_audio_track.mp3

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
